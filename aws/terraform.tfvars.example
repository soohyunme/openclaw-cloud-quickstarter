# === Recommended (New Accounts / 2025 Policy) ===
# t3.small (2GB RAM) is typically FREE for 6 months via credits and much more stable.
# instance_type = "t3.small"
# aws_region    = "us-east-1"  # Optional: Usually set via environment variable in CloudShell
# disk_size_gb  = 30
# disk_type     = "gp3"

# === Legacy / Old Accounts (Before July 2025) ===
# Use t3.micro (1GB RAM) for 12 months free.
# instance_type = "t3.micro"

# Note: LLM Provider and API key are now configured via 'openclaw onboard' 
# after the infrastructure is deployed.